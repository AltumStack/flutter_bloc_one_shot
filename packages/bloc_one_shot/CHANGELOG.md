## 0.2.0

- Add `CompositeEffectObserver` to delegate to multiple `EffectObserver` instances.

## 0.1.0

- Initial release.
- `EffectController` with buffered broadcast stream.
- `SideEffectMixin` for Bloc and Cubit.
- `EffectObserver` for global effect observability.
